# 📜 DOCTRINE (작업 원칙)

슈프림 마스터 아키텍트의 설계 철학을 유지하기 위한 절대 원칙입니다.

## 1. i18n 전략: GeoSmart First
- 모든 다국어 처리는 마스터님이 설계하신 `src/lib/i18n/dictionaries.ts`와 `GeoSmartProvider`를 표준으로 합니다.
- 외부 라이브러리(`next-intl`) 도입 전 반드시 현재의 Flat App 구조와 호환되는지 검증하십시오.
- 서버 컴포넌트에서는 `i18n` 객체에 직접 접근하여 타입 안정성을 확보합니다 (`i18n[locale as Locale]`).

## 2. 배포 전 강제 검증: TSC Zero Tolerance
- 대규모 리팩토링, 라이브러리 추가/제거, 라우팅 수정 후에는 반드시 `npx tsc --noEmit`을 실행하여 전체 프로젝트의 타입 무결성을 확인합니다.
- 빌드 결과가 `Exit code: 0`이 아닐 경우, 절대로 배포 완료를 선언하지 않습니다.

## 3. 라우팅 무결성
- Next.js의 App Router 경로 해석 방식은 파일 시스템과 1:1 대응되어야 합니다.
- 미들웨어는 '보안(Auth)' 및 '최소한의 리다이렉트'에만 집중하며, 복잡한 경로 변조는 가급적 지양합니다.

## 4. 컴포넌트 작성 표준
- **Server Components**: 데이터 페칭 및 딕셔너리 직접 참조.
- **Client Components**: 사용자 인터랙션 및 `useGeoSmart` 훅 활용.
- 두 영역의 경계를 명확히 하고, 불필요한 `'use client'` 남발을 억제합니다.
