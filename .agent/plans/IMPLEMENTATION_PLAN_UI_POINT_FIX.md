# 📋 구현 계획서: UI/UX 고도화 (PPTX 추가) 및 포인트 시스템 무결성 확보

## 🧠 분석 (Analysis)
1. **PPTX 지원 누락**: 엔진은 PPTX를 지원하지만 프론트엔드 배지 및 안내 문구에서 누락되어 있음. (HWP는 지원 대상 제외)
2. **헤더 모바일 가독성**: 모바일 화면에서 언어 선택, 테마, 프로필 메뉴가 밀집되어 레이아웃이 깨지거나 잘리는 현상 발생.
3. **업로드 영역 중첩**: `h-72` 고정 높이 내에서 모바일 수직 배정 시 요소들이 컨테이너 밖으로 넘침.
4. **포인트 차감 이슈**: 번역 수행 후 포인트가 실시간으로 반영되지 않거나 차감되지 않는 현상 점검 필요.
5. **광고 리워드**: 광고 시청 시 포인트가 정상 충전되는지 비즈니스 로직 확인 및 UI 안내 강화.

## 👥 에이전트 할당 (Agent Assignment)
- **@Architect**: UI 가이드라인 수립 및 포인트-광고 리워드 모델 설계 총괄.
- **@The-Builder**: 
    - `page.tsx`: PPTX 배지 추가 및 드롭존 반응형 레이아웃 수정 (고정 높이 제거).
    - `Navbar.tsx`: 모바일 대응을 위한 요소 간격 조정 및 우선순위가 낮은 아이콘 숨김 처리.
    - `api/translate`: 포인트 차감 로직 디버깅.
- **@The-Guardian**: 
    - 포인트 차감/리워드 API 보안 및 무결성 점검.
    - 광고 시청 후 포인트 지급 로직(`PointManager.rewardPoints`) 검증.
- **@The-Reviewer**: 
    - 수정 후 다양한 기기 해상도에서 UI 점검 및 포인트 반영 상태 최종 확인.

## 🛠️ 수정 제안 사항 (Proposed Changes)

### 1. 프론트엔드 UI 수정 (The-Builder)
- **메인 페이지**: 
    - 지원 파일 목록에 `PPTX` 추가. (HWP는 제외)
    - 드롭존 컨테이너를 `h-72`에서 `min-h-[18rem] md:h-72`로 변경하여 모바일 공간 확보.
- **내비게이션 바**:
    - 모바일 환경에서 각 요소 간의 `gap`을 최소화하고, 로고 텍스트를 더 일찍 숨기거나 작게 조정.

### 2. 포인트 및 광고 로직 보완 (The-Builder & The-Guardian)
- **포인트 차감**: 번역 요청 후 성공 시 `PointManager` 호출이 확실히 이루어지는지 확인.
- **광고 리워드**: 사용자가 광고 시청 완료 시 `POST /api/user/reward` (가칭)를 호출하여 포인트를 즉시 반영하는 로직 확인.

### 3. 언어 및 문서 (The-Reviewer)
- 모든 수정 사항은 한국어로 보고하며, UI 문구 역시 다국어 사전에 맞게 업데이트.

## ✅ 검증 계획 (Verification)
1. 해상도 360px 이하에서 헤더 및 업로드 영역 레이아웃 유지 확인.
2. PPTX 파일 선택 시 'ready' 상태로 정상 전환 확인.
3. 번역 후 프로필 드롭다운 내 포인트가 즉시 감소하는지 확인.
4. 광고 시청 시나리오 테스트를 통한 포인트 충전 루프 검증.

---
**상태**: [승인 대기 중]
