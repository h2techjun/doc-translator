import HomePage from "@/app/page";

/**
 * 🚀 All-in-One pSEO Redirect Route
 * Handles:
 * - /translate/en-to-ko/docx (2 segments)
 * - /translate/en/ko/docx (3 segments)
 * - /translate/docx (1 segment)
 */
export default function UniversalTranslatePage() {
    return <HomePage />;
}
